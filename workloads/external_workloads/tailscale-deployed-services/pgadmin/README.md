## Deploying pgAdmin

### Networking

While it would be more convenient to setup the URL as pgadmin.tailscale-url.ts.net, this approach didn't play nicely with the pgAdmin web server, so I set it up as tailscale-server-url.ts.net/pgadmin, which worked fine. 