## Using Portainer to manage K3s/K8s

After I setup a K3s cluster I'll usually setup a Portainer instance on a server external to K3s, as it gives me a way to manage the cluster that isn't dependent on the cluster itself. It's helpful for early setup and for resolving any issues that may occur with tools like Rancher.