apiVersion: v1
kind: Service
metadata:
  name: mariadb-lb
  labels:
    app: mariadb
spec:
  type: LoadBalancer
  ports:
    - port: 3306
      targetPort: 3306
      name: mariadb
  selector:
    app: mariadb
